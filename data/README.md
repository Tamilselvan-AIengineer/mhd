# This folder stores JSON files generated at runtime.
# analysis_history.json  — single-text analysis history
# batch_results.json     — latest batch run results
# suicide_detection.csv  — place your training dataset here (gitignored)
